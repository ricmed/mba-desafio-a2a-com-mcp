# Gere o seu segredo com:
#   python -c "import secrets; print(secrets.token_hex(32))"
# Sao 32 bytes de aleatoriedade em 64 caracteres hexadecimais.
# Nunca comite o valor real: este repositorio e publico.
REQUEST_STATE_SECRET=troque-por-64-caracteres-hexadecimais
